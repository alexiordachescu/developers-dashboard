export const selectSnippetsLoading = (state) => state.snippets.loading;
export const selectSnippetsMessage = (state) => state.snippets.message;

export const selectAllSnippets = (state) => state.snippets.all;
